# Install
    $ bundle install --path bundle

# Bootstrap
    $ git submodule init
    $ git submodule update
[Twitter Bootstrap](http://twitter.github.com/bootstrap/)

# Guard
自動的にPowをrestart
    $ bundle exec guard
